[tox]
envlist = py311, py312, pypy3, py313, py314
isolated_build = False

[testenv]
deps = pytest
skip_install = True
allowlist_externals =
    maturin
commands_pre =
    maturin dev --all-features
commands = pytest -v tests/
